 <button class="btn btn-outline-danger my-2 my-sm-0 {{$user->ban}} ban" {{$user->ban}} name="ban"
                                        id="ban"data-toggle="modal" data-target="#de{{$user->docid}}"><i class="fa fa-times fa-lg"></i>Ban</button> 


            @foreach($users->all() as $user)
        <div class="modal fade" id="ban{{$user->docid}}" tabindex="-1" aria-labelledby="ban{{$user->docid}}Label" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="ban{{$user->docid}}Label">Are you sure to ban?</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-footer">
                        <form method="post" action="{{route('doctorList.ban',$user->docid)}}">
                            @csrf
                            @method('PUT')

                            <input type="submit" value="ban" class="btn btn-outline-primary my-2 my-sm-0"/>
                        </form>
                        <button type="button" class="btn btn-outline-secondary my-2 my-sm-0" data-dismiss="modal">Cancel</button>
                    </div>
                </div>
            </div>
        </div>
        @endforeach